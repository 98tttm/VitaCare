services:
  - type: web
    name: vitacare-backend
    runtime: node
    region: singapore
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://lilhuohuo_db_user:182005@vitacare.vxw5ijd.mongodb.net/VitaCare?retryWrites=true&w=majority&appName=vitacare
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
